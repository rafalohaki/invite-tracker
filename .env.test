# Dummy values used by `bun test` (loaded automatically when NODE_ENV=test).
# Never put real secrets here — this file is committed to the repo.
DISCORD_TOKEN=test-token-not-real
CLIENT_ID=000000000000000000
LOG_LEVEL=ERROR
